<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
      lang="ru"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title%></title>
  </head>
  <body>
    <%- include('./partials/user-header') %> <% if (created) {%>
    <div class="alert alert-success">Замета была создана</div>
    <%}%> <% if (error) {%>
    <div class="alert alert-danger">Что-то пошло не так</div>
    <%}%>

    <div class="container">
      <h1>Заметки на Node</h1>

      <form action="/" method="post" class="mb-2">
        <div class="form-group" class="mb-2">
          <input type="text" class="form-control mb-2" name="title" required />
        </div>

        <button class="btn btn-primary" type="submit">Создать</button>
      </form>

      <% if (notes.length) {%>
      <div class="card">
        <ul class="list-group">
          <% for (let i = 0; i < notes.length; i++) {%>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span class="title"><%= notes[i].title %></span>
            <span class="title"><%= notes[i].owner %></span>
            <% if (notes[i].owner === userEmail) { %>
            <div class="actions">
              <button class="btn btn-primary" data-type="edit" data-id="<%=notes[i].id%>">
                Редактировать
              </button>
              <button
                class="btn btn-danger"
                data-type="remove"
                data-id="<%=notes[i].id%>"
              >
                &times;
              </button>
            </div>
            <%}%>
          </li>
          <%}%>
        </ul>
      </div>
      <%} else {%>
      <p>Заметок нет</p>
      <%}%>
    </div>
    <script src="/app.js"></script>
  </body>
</html>
